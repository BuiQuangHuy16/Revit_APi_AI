<Window x:Class="Aplication.Commands.ExportSchedule.Views.ExportScheduleWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:Aplication.Commands.ExportSchedule.ViewModels"
        Title="Export Schedule to Excel — MV_tool"
        Width="520"
        Height="680"
        MinWidth="420"
        MinHeight="500"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize"
        ShowInTaskbar="False"
        FontFamily="Segoe UI"
        FontSize="13"
        Background="#FFFFFF">
    <Window.Resources>
        <!-- ====== Palette ====== -->
        <SolidColorBrush x:Key="AccentBrush" Color="#A02828"/>
        <SolidColorBrush x:Key="AccentHoverBrush" Color="#882020"/>
        <SolidColorBrush x:Key="MutedBrush" Color="#8A8A8A"/>
        <SolidColorBrush x:Key="BorderBrush" Color="#D0D0D0"/>
        <SolidColorBrush x:Key="PanelBrush" Color="#FFFFFF"/>

        <!-- ====== TextBox phẳng có placeholder ====== -->
        <Style x:Key="FlatTextBox" TargetType="TextBox">
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
        </Style>

        <!-- ====== Button thường (Check All / None / Cancel) ====== -->
        <Style x:Key="FlatButton" TargetType="Button">
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="Foreground" Value="#333333"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="14,6"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4"
                                SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#F4F4F4"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ====== Nút Export nổi bật (Accent) ====== -->
        <Style x:Key="AccentButton" TargetType="Button" BasedOn="{StaticResource FlatButton}">
            <Setter Property="Background" Value="{StaticResource AccentBrush}"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="20,8"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4"
                                SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              Margin="{TemplateBinding Padding}"
                                              TextBlock.Foreground="{TemplateBinding Foreground}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{StaticResource AccentHoverBrush}"/>
                                <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource AccentHoverBrush}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.45"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ====== Template từng dòng schedule ====== -->
        <DataTemplate DataType="{x:Type vm:ScheduleItemViewModel}">
            <StackPanel Orientation="Horizontal" Margin="2,3">
                <CheckBox IsChecked="{Binding IsSelected, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                          VerticalAlignment="Center"
                          Margin="0,0,8,0"/>
                <TextBlock Text="{Binding Name}" VerticalAlignment="Center"/>
            </StackPanel>
        </DataTemplate>
    </Window.Resources>

    <Grid Margin="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Row 0: Search box (placeholder hiển thị khi TextBox rỗng) -->
        <Grid Grid.Row="0" Margin="0,0,0,8">
            <TextBox x:Name="SearchTextBox"
                     Style="{StaticResource FlatTextBox}"
                     Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"/>
            <TextBlock Text="Search schedule name..."
                       Foreground="{StaticResource MutedBrush}"
                       FontStyle="Italic"
                       VerticalAlignment="Center"
                       Margin="10,0,0,0"
                       IsHitTestVisible="False">
                <TextBlock.Style>
                    <Style TargetType="TextBlock">
                        <Setter Property="Visibility" Value="Collapsed"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding Text, ElementName=SearchTextBox}" Value="">
                                <Setter Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>
        </Grid>

        <!-- Row 1: Check All / None + Total count -->
        <Grid Grid.Row="1" Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0"
                    Style="{StaticResource FlatButton}"
                    Content="Check All"
                    Command="{Binding CheckAllCommand}"
                    Margin="0,0,6,0"/>
            <Button Grid.Column="1"
                    Style="{StaticResource FlatButton}"
                    Content="Check None"
                    Command="{Binding CheckNoneCommand}"/>

            <TextBlock Grid.Column="3"
                       VerticalAlignment="Center"
                       Foreground="{StaticResource MutedBrush}">
                <Run Text="{Binding TotalCount, Mode=OneWay}"/><Run Text=" item(s)"/>
            </TextBlock>
        </Grid>

        <!-- Row 2: Danh sách schedule -->
        <Border Grid.Row="2"
                BorderBrush="{StaticResource BorderBrush}"
                BorderThickness="1"
                CornerRadius="4"
                Background="{StaticResource PanelBrush}">
            <ListBox ItemsSource="{Binding FilteredItems}"
                     BorderThickness="0"
                     Background="Transparent"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                     ScrollViewer.VerticalScrollBarVisibility="Auto"
                     VirtualizingStackPanel.IsVirtualizing="True"
                     VirtualizingStackPanel.VirtualizationMode="Recycling"
                     HorizontalContentAlignment="Stretch"
                     SelectionMode="Single"
                     Padding="6"/>
        </Border>

        <!-- Row 3: Footer (separate-files + Export + Cancel) -->
        <Grid Grid.Row="3" Margin="0,12,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <CheckBox Grid.Column="0"
                      Content="Export to separate files (.xlsx)"
                      IsChecked="{Binding ExportToSeparateFiles, Mode=TwoWay}"
                      VerticalAlignment="Center"/>

            <Button Grid.Column="1"
                    Style="{StaticResource AccentButton}"
                    Content="{Binding ExportButtonText}"
                    Command="{Binding ExportCommand}"
                    MinWidth="120"
                    IsDefault="True"
                    Margin="0,0,8,0"/>

            <Button Grid.Column="2"
                    Style="{StaticResource FlatButton}"
                    Content="Cancel"
                    Command="{Binding CancelCommand}"
                    MinWidth="90"
                    Padding="20,8"
                    IsCancel="True">
                <Button.Foreground>
                    <SolidColorBrush Color="#A02828"/>
                </Button.Foreground>
                <Button.BorderBrush>
                    <SolidColorBrush Color="#A02828"/>
                </Button.BorderBrush>
            </Button>
        </Grid>
    </Grid>
</Window>
